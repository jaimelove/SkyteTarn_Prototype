using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurretActivatorZone : MonoBehaviour
{
    /*
    private void OnTriggerEnter2D(Collider2D other)
    {
        Debug.Log(other);
        if (other.gameObject.GetComponent<Turret>())
        {
            Debug.Log(other.gameObject + " is active");
            other.gameObject.GetComponent<Turret>()._isActive = true;
        }
    }

    private void OnTriggerExit2D(Collider2D other)
    {
        if (other.gameObject.GetComponent<Turret>())
        {
            Debug.Log(other.gameObject + " is not active");
            other.gameObject.GetComponent<Turret>()._isActive = false;
        }
    }
    */
}
